# Frontend — Next.js (ImobiCrm)

Pasta: `frontend`

Instalação e execução em desenvolvimento:

```bash
cd frontend
npm install
npm run dev
```

Por padrão o frontend consome a API em `http://localhost:5000`. Para alterar, exporte a variável `NEXT_PUBLIC_API_URL`.
